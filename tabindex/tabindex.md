tabIndex 是 HTML 的一个全局属性，规定了是否可以通过 tab 键来获得焦点，以及导航顺序。

它的取值是一个数字：

- -1，不会出现在 tab 键导航序列中，但是可以通过编程的方式 focus
- 0，默认值，使得不可导航的元素（如 div, span）变得可以导航，导航顺序是 DOM 中的自然顺序
- 正整数：自定义导航顺序，数字越小，越先被 tab 到，会在 0 之前被 tab 到，不推荐使用，会破坏用户的预期，难以维护
